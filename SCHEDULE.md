# Timers

Last-said: run timers. Not a zoo of extra Groks.

| Timer | Interval | Does | Does not |
|---|---|---|---|
| Grok coord | 1 hour | Read Current, check `Downloads` + `inbox/` for new conversation dumps, update INFORM, keep this repo and `ai-coordination` honest | Mail, spend, invent secrets, headed browser, `grok -p` |
| Pages | on git push | GitHub Actions deploys this folder | Rewrite the conversation |
| Credit pass | 4 hours | If a mouth is idle, write **one** concrete job into `jobs.md` status; do not start a second Talk | Buy seats, clone mouths |

Human gates stay human: GitHub login, Pages click if API cannot enable, Claude/Codex/Kimi/Gemini sign-in, dad URL, Send, Indeed.

New dumps are copied to `inbox/`. They go live only when Danny says publish.
